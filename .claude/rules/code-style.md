---
paths: '**/*.{php,vue,js,ts}'
---

# Formatting Guardrails (Short)

- Follow Prettier/ESLint output and `.editorconfig` (4-space indent).
- JS/TS/Vue: semicolons, single quotes.
- Keep imports organized by tooling (organize-imports).
- Vue SFCs use `<script setup>`.

Details: `docs/rules/code-style.md`.
