# name: Deploy to Production Server

# on:
#   push:
#     branches: [main, production]
#   workflow_dispatch:

# env:
#   REGISTRY: ghcr.io
#   IMAGE_NAME: ${{ github.repository }}

# jobs:
#   test:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup PHP
#         uses: shivammathur/setup-php@v2
#         with:
#           php-version: 8.4
#           tools: composer:v2

#       - name: Setup Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: '22'
#           cache: 'npm'

#       - name: Install Dependencies
#         run: |
#           composer install --no-interaction --prefer-dist --optimize-autoloader
#           npm ci

#       - name: Copy Environment File
#         run: cp .env.example .env

#       - name: Generate Application Key
#         run: php artisan key:generate

#       - name: Publish Ziggy Configuration
#         run: php artisan ziggy:generate

#       - name: Build Assets
#         run: npm run build

#       # - name: Run Tests
#       #   run: ./vendor/bin/pest

#       - name: Run Linting
#         run: |
#           vendor/bin/pint --test
#           npm run lint
#           npm run format:check

#   build-and-push:
#     needs: test
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write

#     outputs:
#       image: ${{ steps.image.outputs.image }}

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Log in to Container Registry
#         uses: docker/login-action@v3
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Extract metadata
#         id: meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
#           tags: |
#             type=ref,event=branch
#             type=ref,event=pr
#             type=sha,prefix={{branch}}-
#             type=raw,value=latest,enable={{is_default_branch}}

#       - name: Build and push Docker image
#         id: build
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           platforms: linux/amd64,linux/arm64
#           push: true
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max

#       - name: Output image
#         id: image
#         run: echo "image=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}" >> $GITHUB_OUTPUT

#   deploy:
#     needs: build-and-push
#     runs-on: ubuntu-latest
#     environment: production

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Deploy to production server
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.PRODUCTION_HOST }}
#           username: ${{ secrets.PRODUCTION_USER }}
#           key: ${{ secrets.PRODUCTION_SSH_KEY }}
#           port: ${{ secrets.PRODUCTION_PORT || 22 }}
#           script: |
#             # Navigate to application directory
#             cd ${{ secrets.PRODUCTION_APP_PATH || '/opt/swinx' }}

#             # Pull latest code
#             git pull origin main

#             # Update image tag in production environment
#             echo "DOCKER_IMAGE=${{ needs.build-and-push.outputs.image }}" > .env.docker.image

#             # Run deployment script
#             ./scripts/deploy.sh

#       - name: Health check
#         run: |
#           sleep 30
#           curl -f ${{ secrets.PRODUCTION_URL }}/health || exit 1
#           echo "🚀 Application deployed successfully to: ${{ secrets.PRODUCTION_URL }}"
